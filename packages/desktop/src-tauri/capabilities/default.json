{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Core permissions for the main window. The opener entries carry an explicit scope: its commands check `allowed.iter().any(...)`, so a bare `allow-open-path` grants the command and permits no path — every open silently failed. The omp sidecar is owned entirely by Rust, so no shell:* permission is granted: the webview must not be able to spawn it directly and bypass the relay.",
	"windows": [
		"main"
	],
	"permissions": [
		"core:default",
		"core:window:allow-start-dragging",
		"dialog:allow-open",
		"dialog:allow-save",
		"clipboard-manager:allow-read-text",
		"clipboard-manager:allow-write-text",
		"opener:default",
		{
			"identifier": "opener:allow-open-path",
			"allow": [
				{
					"path": "**"
				}
			]
		},
		{
			"identifier": "opener:allow-reveal-item-in-dir",
			"allow": [
				{
					"path": "**"
				}
			]
		}
	]
}
